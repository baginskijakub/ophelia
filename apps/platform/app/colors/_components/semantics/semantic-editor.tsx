import { cx } from "cva";

export const SemanticEditor = () => {
  return (
    <div
      className={cx(
        "absolute top-0 right-0",
        "w-64 h-full p-3",
        "bg-primary border-l-[0.5px] border-primary-style",
        "flex flex-col",
      )}
    >
      neutral-900
    </div>
  );
};
