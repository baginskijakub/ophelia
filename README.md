this repo is based on turborepo from vercel

---

to run all projects in dev env you can run npm run dev in root dir. this will run all projects with hot reload

---

to add new app simply create a dir in `/apps` and just copy configs from a different app, so far there's just a next app and client side storybook
